import reducer from "./budgetReducer"

const initialState = {
    email: null,
    firstName: null,
    lastName: null
}

export default function userReducer(state = initialState, action){
    return state;
}